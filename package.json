{
  "name": "orgpressor",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean",
    "pack": "mkdir -p packages && cd apps/orgpressor-ui && pnpm pack --pack-destination ../../packages"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "pnpm@9.0.0"
}
